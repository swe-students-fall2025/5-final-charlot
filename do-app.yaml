name: charlot-ml-app
region: nyc

services:
  - name: backend
    dockerfile_path: ./backend/Dockerfile
    http_port: 3000
    instance_size_slug: basic-xxs
    instance_count: 1

  - name: ml-client
    dockerfile_path: ./ml-client/Dockerfile
    http_port: 8000
    instance_size_slug: basic-xxs
    instance_count: 1
